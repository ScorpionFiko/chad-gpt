import React from "react";

function DashMessage() {
    return (
        <div>
            <p>This will be the welcome message with personalized features from user info</p>
        </div>
    );
};

export default DashMessage;